# Assisted fetch of one cited Ancestry record, then the loop end to end

You are the worker session. Read `CLAUDE.md`, `MEMORY.md`, and `docs/AUDIT-PROMPT.md` first. The owner is logged into Ancestry in Chrome and the Claude in Chrome tools are available to you. Your job is the assisted fetch of one cited record, so the extractor can be verified on a real page, then the loop end to end. Limits: fetch only records the tree already cites, one at a time, in the owner's own browser session, never crawl or search Ancestry, and stop and report if any page blocks, rate-limits, or asks for anything.

1. On a scratch copy of the catalog, find the fetch step for Abram C. Brant's 1900 census household. Its locator is an Ancestry record id; the person screen builds the record URL from it.
2. Open that URL in a new tab. Confirm the page shows the record. Capture the rendered page as HTML through the page's own DOM (the full document markup), chunking if it is large, and write it into `inbox/` as a `.html` file named after the collection and record id. Do not use a browser save dialog.
3. Open the record's image viewer and use its own download control so the original image file lands in the browser's download folder; move that file into `inbox/`. If the viewer has no download control, capture the image by fetching its source URL from within the page and writing the bytes into `inbox/`; if that fails, save a full-resolution screenshot and say plainly that it is a screenshot, not the original.
4. Close the tab. Do not open any other Ancestry page.
5. On a scratch copy: archive the page through the screen's attach path, run `tools/extract.py`, compare field by field to the page, fix the parser until every label, value and household member is captured as written, run the matcher. Then: accepting a persona match, or the fact decision that follows it, closes every open question on that person the record answered, with `closed_reason` answered and `answered_by_proposal_id` set (`missing_parents` when a parent link is accepted; `unverified_claim` when the uncited event gains an accepted assertion; `missing_fact` when the fact now has a value with an accepted assertion; `conflict` only by a person), and regenerates the person's plan in the same request. New-person proposals become decidable: accepting creates the person in this tree, the persona link accepted, the same Undecided assertions as a match, and the family membership with an Undecided assertion when the persona's relation says child, parent or spouse of a person matched on the record; rejecting writes the proposal rejected and nothing else. Superseding an extraction marks its undecided proposals rejected with a note saying superseded. Commit per phase with the data-file guard. The saved page and image never enter git.
6. Test on scratch copies and report verbatim: the extraction and the matcher's proposals from the real page; a match accepted with the question closing and the plan regenerating; a new-person proposal accepted with the person, link and assertions; one rejected; a superseded extraction; `plan.py --all` twice unchanged; integrity and foreign-key checks. Add the exact page structure found, whether the image is the original or a screenshot, and one line stating whether the parser was verified on a real page.
